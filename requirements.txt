# Core dependencies
streamlit==1.38.0
ultralytics==8.3.0
pillow>=10.0.0
numpy==1.24.3

# Deep learning framework (PyTorch) - CPU only for cloud
torch==2.0.1
torchvision==0.15.2

# Computer vision (use headless version for cloud deployment)
opencv-python-headless==4.8.1.78

# Serialization (required for PyTorch model loading)
dill==0.3.7
