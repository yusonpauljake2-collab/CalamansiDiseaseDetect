# Core dependencies
streamlit>=1.38.0
ultralytics>=8.0.0
pillow>=10.0.0
numpy>=1.24.0,<2.0.0

# Deep learning framework (CPU-only for cloud deployment)
torch>=2.0.0,<2.2.0
torchvision>=0.15.0,<0.17.0

# Computer vision (headless for server environments)
opencv-python-headless>=4.8.0

# Serialization
dill>=0.3.0

# Optional: for production stability
watchdog>=2.1.0
